definition_version: 1
streamlit:
  name: sample_app
  query_warehouse: compute_wh # Note that an XS warehouse is recommended
  main_file: streamlit_app.py
  env_file: environment.yml
  database: streamlit_apps
  schema: public
  #pages_dir: pages/
  #additional_source_files:
  #  - common/*.py
